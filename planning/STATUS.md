# 진행 기록

## 2026-09-25 — 채용용 화면 다듬기 (portfolio-polish 브랜치, 배포 전)

- 사용자가 사이트 전체 검토를 요청했고, 검토 결과 가운데 1단계(화면만)와 2단계(구조)를 승인했다. 과제가 정한 칸(이름, "…한 사람" 한 줄 소개, 이야기 첫·끝 문장, 고난→다시 일어난 날→지금 순서, 숫자와 출처)과 문장은 바꾸지 않았다.
- 구조: 섹션 순서를 대표작 → 이야기 → 숫자 → 경력 → 문서로 바꿨다. 대표작 시작 위치가 1440px에서 2,552px → 84px, 390px에서 4,678px → 1,035px가 됐다. 대표작과 같은 경력 세 개(CLOV·10번·5번, `workId`)는 상황·행동·결과를 빼고 화면과 결과 한 줄, "대표작에서 자세히 보기"만 둔다. 전체 상황·행동·결과는 경력기술서 문서에 그대로 있다.
- 화면: 오른쪽 섹션 레일은 프로필 카드의 섹션 목록이 화면 밖으로 나간 뒤에만 보인다(두 목록이 동시에 보이지 않음, HTML에는 둘 다 남음). 대표작 글 칸을 넓히고 제목을 22~28px로 줄였다. 그림 덮개를 아래쪽만 옅게, 번호를 40px로 줄였고, 10번·13번 그림은 왼쪽 위를 초점으로 둔다(`visual.focus`). 숫자 카드의 큰 숫자·막대 높이를 맞췄다. 휴대폰 하단 바를 불투명하게 하고, 프로필 카드의 `isolation` 때문에 뒤 본문이 바 위로 그려지던 쌓임 문제를 고쳤다(56개 스크롤 위치에서 확인).
- 맨 아래 "마지막 갱신 2026-09-20"은 숫자 기준일(`updatedAt`, 장치가 출처 기준일의 최댓값으로 넣는 값)이라 날짜는 두고 이름을 "숫자 기준일"로 바꿨다.
- 제출 ZIP의 `submission/README-FIRST.md`는 제출 당시 순서(이야기·숫자·대표작)를 적은 기록이라 고치지 않았다.
- 검사: 단위 20개, 반복 생성, 외부 URL 포함 릴리스 검사 통과. 1440·1280·390 폭과 어두운 화면에서 가로 넘침 0.

## 2026-09-22 — ExplainSOC 비전공자 첫 관찰 반영

- 처음 본 비전공자가 무엇을 하는 사이트인지 이해하지 못했다는 관찰을 반영해, 13번 앱이 첫 화면을
  쉬운 말과 구체적인 사용 상황으로 고쳤다(`b5384ee`).
- 12번 대표 카드의 한 문장도 `평가 조건·공급자` 중심 표현에서 `보안 AI 광고 숫자를 확인하고 판매
  업체에 물어볼 질문을 만든다`는 표현으로 바꿨다. 문제 사실·기능·검증 수·그림·링크는 바꾸지 않았다.

## 2026-09-22 — ExplainSOC 대표 카드 V5 결과 브리핑

- 13번 앱의 첫 결과를 `Decision Brief`로 재구성한 V5에 맞춰 대표 그림을
  `docs/assets/t13-decision-brief-v5.png`로 바꿨다. 판정 도구가 아니라 현재 근거에서 먼저 읽을
  해석 주의와 다음 질문을 제시하는 제품 원칙이 카드 한 장에서도 보인다.
- 판단 문장에는 결과를 읽는 순서를, 구현 문장에는 Decision Brief와 모바일 장면 전환을 추가했다.
  검증 수는 V5 구현 커밋 `7d0a105`의 단위·컴포넌트 92개, 실제 브라우저 26개로 고쳤다.
- 이름·한 문장·문제 사실·공개 링크는 바꾸지 않았다.
- 사이트 재생성, 단위 검사 20개, 반복 생성, 외부 URL을 포함한 릴리스 검사와 제출 ZIP 생성을
  모두 통과했다.

## 2026-09-22 — ExplainSOC 대표 카드 V3 검증 수 갱신

- 13번 앱 V3의 현재 기능과 검증 결과에 맞춰 대표 카드의 판독 규칙을 13개에서 14개로, 구현 설명에
  회차 비교·JSON 저장과 열기·자료 요청서를 더하고, 단위·컴포넌트 테스트를 80개에서 91개로,
  실제 브라우저 테스트를 22개에서 24개로 고쳤다.
- 근거는 `explainsoc` 저장소의 V3 구현 커밋 `679add3`와 저장소 밖 빈 폴더에서 다시 확인한
  `explainsoc-679add3.zip`이다.

## 2026-09-22 — ExplainSOC 대표 카드 V2 갱신

- 13번 앱이 PoC 검토 작업대 V2로 배포되어 `t13-app` 카드의 판단·구현·검증 문구를 현재 기능에 맞춰 고쳤다.
- 판독 규칙 13개, 평가 근거 지도, 질문 상태·메모, PoC 검토표 복사·PDF, Vitest 80개·Playwright 22개를 반영했다.
- 대표 그림을 V1 숫자 뒤집기 단일 패널에서 V2 작업대 전체가 보이는 `docs/assets/t13-workbench-v2.webp` 900×1200으로 바꿘습니다.
- 사이트 재생성, 단위 검사 20개, 반복 생성, 외부 URL을 포함한 릴리스 검사를 통과하고 1440px 카드를 눈으로 확인했다.

## 2026-09-22 — 13번 앱을 대표작 넷째 자리에 연결 (제출 뒤 갱신)

13번 앱 ExplainSOC가 공개되고 새 시크릿 창·새 임시 폴더 확인을 마쳐서(`explainsoc` 저장소
`evidence/card-4.md`), 제출 뒤 할 일 첫째를 했다. 제출본(`a413189`)의 13번 자리는 예정 카드였고,
지금 사이트에서는 공개 카드다.

- `approved.json`의 `t13-app`: `planned` → `published`. `plannedDate`를 빼고 기간 2026.09, 이름
  "ExplainSOC — 성능표에 없는 질문", 한 문장(누구를 어떻게 돕는가 — 앱 첫 화면 도움 문장에 "10번
  논문"만 더함), 사례 노트 넷, 그림, 링크 셋(앱 체험·검증 기록·공개 저장소)을 채웠다. 숫자와 검증
  결과는 13번 저장소의 증거에 있는 것만 썼다.
- 그림 `docs/assets/t13-claim-flip.webp`(900×1200): 공개 주소에서 찍은 "숫자 뒤집어 보기" 칸.
  대표작 칸이 데스크톱에서 세로로 길어 가로 화면을 넣으면 가운데만 남아 "공격 220,788건 가운데 탐지
  160건"이 잘렸다. 세로 3:4로 다시 찍어 데스크톱·모바일·왼쪽 카드 세 곳에서 그 문장이 온전히 보이게
  했다.
- 왼쪽 카드가 7장에서 8장이 됐다. 겹침 간격 검사(마지막 카드 top + 330px ≤ 560px)는 8장까지
  들어간다(210 + 330 = 540). 카드 강조색은 6장까지만 있어 7·8번째는 기본 파랑이다(이전부터 7번째가
  그랬다).
- 테스트: 사례 노트 수 3 → 4, 단계 12 → 16으로 고치고, 13번 카드가 왼쪽 카드와 대표작에 모두
  있는지 확인하는 줄을 더했다. 문서 3종은 대표작을 쓰지 않아 본문이 그대로다(DOCX는 다시 만들어
  본문 비교 후 원래 파일로 되돌렸다). 제출한 ZIP(`release/out/`)은 덮지 않았다.
- 확인: 단위 20개 OK, `check_repeat` PASS, `validate_release --check-urls` PASS, 릴리스 ZIP 생성 PASS
  (임시 경로).
- 같은 날 13번에 단위 테스트가 하나 늘어(74 → 75, 데이터셋 인용에서 저자 실명을 뺀 검사) 사례 노트의
  검증 칸 숫자를 75개로 고쳤다.

## 2026-09-21 — 제출

`a413189` 상태로 T12를 제출했다. 결과물 URL은 `https://myeongjundev.github.io/about/`, 파일은
`T12-KimMyeongjun.zip`(44개 항목)과 이력서·자기소개서·경력기술서 DOCX 셋이다. 재현·통과
확인 4가지와 AI와 내 판단 3줄은 `AI-JUDGMENT.md`와 `CHECK-HOWTO.md`를 줄여 양식에 적었다.
제출한 것과 양식 문안, 제출 전 확인 내용은 [`SUBMISSION-2026-09-21.md`](SUBMISSION-2026-09-21.md)에 있다.

제출 뒤 할 일은 둘이다.

- 13번을 마치면 `approved.json`의 `t13-app`을 `published`로 바꾸고 이름·그림·링크·검증 결과를
  채운다(예정 · 2026.11.12 이전).
- 새 기록이 쌓이면 `private/inputs/`로 장치를 돌려 숫자 칸을 갱신한다(지금 기준일 09-17·09-20).

## 2026-09-21 — 이력서 교육에 국비 과정 추가, 쪽 채움 값 바로잡기

교육 칸에 빠져 있던 국비 과정을 넣었다. SKT ALEPH와 학사 사이, 최근 순이다.

- 생성형 AI 활용 실버케어 PHR 솔루션 개발 교육과정 · 코리아AI아카데미 부산점 ·
  2026.03 - 2026.08 (본인이 준 기간은 03.24 ~ 08.14, 다른 항목과 같이 월 단위로 적었다)
- 설명 한 문장은 본인 문장 그대로이고, 끝만 이력서 말투에 맞춰 "수행했습니다"로 맺었다.

교육 칸은 이름과 기간만 그렸다. `org`와 `detail`을 선택 필드로 더해 이력서에는 기관·내용
줄이, 사이트 왼쪽 카드에는 기관 이름이 붙는다. 이력서는 2쪽 그대로이고 2쪽 아래에 여백이
남는다. 자기소개서와 경력기술서는 교육을 싣지 않아 그대로다.

**쪽 채움 값을 잘못 읽고 있었다.** `render_docx.py`의 "마지막 쪽 채움"은 세로 여백이 아니라
마지막 쪽 글자 수를 앞 쪽들 가운데 가장 많은 쪽과 견준 비율이다. 그래서 이번 이력서는 1.156이
나와도 넘치지 않았고, 1쪽 문서는 늘 1.0이다. 앞 기록의 "이력서 0.972라 한두 줄만 늘어도
3쪽", "자기소개서 1.0이라 한 줄만 늘어도 2쪽"은 이 값으로 알 수 없는 말이라 지웠다. 쪽이
넘치는지는 PDF를 직접 보고 확인한다.

## 2026-09-21 — 13번 앱 자리와 숫자의 근거 능력

제출 전 과제 원문과 다시 대조해 두 가지를 바로잡았다.

- **13번 앱 예정 자리**(`2845ee7`). BRA-C06이 "13번 앱이 들어갈 자리"를 요구해 대표작 넷째에
  예정 카드(NEXT, "예정 · 2026.11.12 이전")를 되살렸다. 09-20의 "완료된 결과만" 결정은 원문과
  어긋났다.
- **숫자가 뒷받침하는 힘**. 과제는 "회복탄력성과 과제지속력은 13주 기록의 숫자로 뒷받침한다"고
  하는데, 사이트 어디에도 두 말이 없었다. `DESIGN.md`에는 짝이 있었지만 사이트를 만들며 빠졌다.
  숫자마다 `supports`를 두어 카드 출처 줄 맨 앞에 "근거 · …"로 보이게 했다. 출석과 제출은
  과제지속력, 매일 다시 시작한 아침 리추얼은 회복탄력성이다(본인 확정). 숫자 칸 설명도
  "회복탄력성과 과제지속력을 13주 과정 기록의 숫자로 보여 줍니다"로 바꿨다. 이미 대비 점검을
  통과한 `.source` 스타일 안에 넣어 새 스타일은 없다. 세 카드 높이가 같고 1440px·375px에서
  가로 넘침이 없다. 값이 비면 릴리스 검사가 실패한다.

문서 셋은 두 변경 모두 싣지 않아 그대로다.

## 2026-09-21 — 제출 전 검토

제출 ZIP을 지금 커밋으로 다시 만들어(44개 항목, 고정 타임스탬프) 채점자처럼 검토했다.

- 빈 폴더 + 시스템 Python 3.13(venv 아님)에서 `refresh`·`check_repeat`가 통과하고 출력이
  `device/last-result/`와 같다.
- ZIP 전체(문서 본문 포함)에서 이메일·전화번호·키·토큰·Webhook 주소·수업 계정 흔적 0건.
- 사이트와 문서의 링크 15개 모두 200, 로그인 페이지로 넘어가는 곳 없음.
- 5·4·2·8·10번의 숫자를 각 저장소 README와 대조해 모두 일치.

고친 것 둘.

- **ZIP에서 안 도는 명령이 안내문에 섞여 있었다.** `CHECK-HOWTO`는 `device/README.md`를
  따르라고 하는데, ZIP에는 `content/`와 `release/`가 없어 `apply_numbers`·`build_site`·
  `validate_release`·`test_device`가 멈춘다. 원래부터 그랬다. 스크립트는 그대로 두고
  `device/README.md`에 "어디서 실행하나" 표를 넣어 ZIP에서 도는 두 명령과 저장소에서만
  도는 명령을 나누고, 해당 절에 "(저장소에서만)"을 달았다. `CHECK-HOWTO` 2번도 그 표를
  가리키게 했다.
- **문서가 DOCX뿐이라고 적혀 있었다.** ZIP에는 PDF도 들어 있어 `README-FIRST`와
  `CHECK-HOWTO`를 "DOCX와 PDF"로 고쳤다.

남은 것은 본인이 하는 제출 두 가지다. 결과물 URL 칸에 `https://myeongjundev.github.io/about/`
하나(BRA-C20), 문서와 `release/out/T12-KimMyeongjun.zip`을 파일로 올리기(BRA-C21).

## 2026-09-21 — 배포본 확인과 게시판 저장소 정리

수업 실습 게시판을 넣은 뒤(`a4adea3`) 공개 사이트와 문서 셋을 배포본으로 다시 봤다.
모두 로컬 빌드와 바이트까지 같다.

- **사이트**: 새 항목이 경력 05번(5번 과제 다음)에 나오고, 그림 두 장이 제 크기로 뜬다.
  1440px와 375px에서 가로 넘침이 없다. 휴대폰에서 그림은 다른 항목처럼 옆으로 미는 줄이다.
  사이트는 경력 항목의 판단 줄을 보여 주지 않으므로 판단은 경력기술서에서만 확인한다.
- **경력기술서 4쪽**: 새 항목은 3쪽 첫머리에 온전히 들어갔다. 항목을 쪽 중간에서 자르지
  않아 2쪽 아래 4분의 1쯤이 비는데, 1쪽 아래가 비던 것과 같은 이유라 **그대로 두기로 했다.**
  항목을 쪼개면 읽기 어려워진다.
- **이력서 2쪽**: `그 외 작업` 절 이름과 새 항목(제목·기간·결과·링크)이 들어갔다.
- **자기소개서 1쪽**: 이번 추가로 바뀐 것이 없다(`841dd6a` 때와 같은 파일).
- **공유 미리보기**: `og:image`(1200×630 PNG)와 `twitter:card`가 배포본에 있고 그림은 200이다.
  테스트로는 알 수 없는 실제 표시는 본인이 카카오톡에 링크를 붙여 확인했고 문제없이 떴다.

문서 문장을 고치면 인계 문서 4절의 일곱 단계를 돌리고, 쪽 수는 PDF를 직접 보고 확인한다.

게시판 저장소(`myeongjundev/flask-board`)도 포트폴리오에서 링크되므로 정리했다. SYN Flood
실습을 실제 탐지값이 흐르는 상태로 다시 검증해 캡처를 바꿨고(`0221b40`), README 맨 위에
실습 목록을 두고 테스트 명령을 실제로 도는 `unittest`와 45개로 고쳤다(`2943435`).

## 2026-09-21 — 수업 실습 게시판을 경력에 추가

학원 수업의 Flask 게시판(`myeongjundev/flask-board`)을 경력 목록에 "수업 실습"으로
넣었다. 과제가 아니므로 번호를 붙이지 않았고, 대표작 카드도 달지 않았고, 숫자 칸의
"제출한 과제 11/11"에도 섞지 않았다. 5번 과제 다음, 간단히 싣는 항목(compact)의 둘째다.

- **판단 줄은 본인 것이 날짜로 증명되는 것을 골랐다.** 자기 강등·자기 삭제·마지막 관리자
  강등 금지는 09-10(`27c75c4`)에 만들었고, 강사님의 과잉권한 자동 회수 코드를 들여온 것은
  09-17(`c5e4061`)이다. 들여올 때 같은 규칙을 회수 봇에도 씌웠다. 회수 흐름 자체는 수업
  코드라 `scope`에 "수업에서 받은 … 코드의 이식"으로 밝혔다.
- **회수 봇 3단계와 비밀값 노출 막기는 본인이 넣은 것**이라고 본인이 확인했다.
- **결과 줄의 숫자는 오늘 다시 쟀다.** 게시판 저장소 테스트 45개(unittest). SYN Flood는
  n8n 실행 185번, 게시판 id 26의 35,033개. 게시판 저장소 README에 있던 옛 수 8개는 쓰지
  않았다.
- **그 SYN 실습을 올리기 전에 고쳤다.** 공개 저장소 캡처에 공격이 ✅ [허용]으로 찍히고
  학생 식별자가 수업 자료 계정이었으며, Graylog 알림이 실제 출발지와 SYN 개수를 싣지
  않았다. 고친 뒤 캡처를 다시 찍었다(`flask-board` `0221b40`).
- 능력은 **자기조절력**. 권한을 뺏는 기능을 바로 켜지 않고 확인 → 신고 → 실제 회수로
  나눈 절제가 중심이라서다.
- 그림 둘: 관리자 자기 강등 차단 화면(판단 줄 그대로)과 SYN 경보 게시판 기록.

지면이 늘었다. 경력기술서가 3쪽에서 **4쪽**(마지막 쪽 0.373)이 되어 테스트 한도를 3에서
4로 올렸다. 사이트는 경력 항목의 판단 줄을 보여 주지 않아 판단은 경력기술서에만 나오므로,
항목을 경력기술서에서 빼는 대신 한도를 올렸다. 이력서는 2쪽을 지켰다. 자기소개서는 그대로다.

이력서의 `그 외 과제` 절은 수업 실습이 들어가면서 **`그 외 작업`**으로 이름을 바꿨다.
`AI-JUDGMENT.md`의 "공개한 작업 여섯 개 중 … 나머지 셋"도 일곱·넷으로 고쳤다.

## 2026-09-21 — 인계 문서의 본인 확인 셋을 끝내고 제출 기준을 다시 점검

집에서 넘긴 `HANDOFF-2026-09-21.md`의 1~3번을 학원 PC에서 처리했다.

- **AI-JUDGMENT의 판단 절을 고쳤다.** "내가 판단한 일"이 무엇을 확인했다는 절차 설명이라
  무엇을 싣고 뺐는지로 바꿨다. 공개한 작업 여섯 중 앞선 셋만 자세히 쓴 것, 3번을 5번으로
  교체한 것, 확인되지 않은 값을 뺀 것을 적었다. 5번에서 다음 AI의 절차 위반을 통과 결과로
  덮지 않기로 한 판단도 한 문장 더했다 — 5번 제출문과 겹치지만 결이 달라 남겼다.
  "맡긴 일"에는 요구사항 정리를 남기고 문서 3종과 이를 한 파일에서 만들어 내는 장치,
  눈으로 세기 어려운 측정을 맡긴 사실을 적었다.
- **판단 줄 넷을 각 저장소 README와 대조해 4번만 고쳤다.** 8·5·2번은 README 문장과 맞아
  그대로 뒀다. 4번은 사실이 틀리진 않았지만 README가 "내가 판단한 일"로 적어 둔 것은 출처
  선택(Stack Overflow 대신 Steam, 네 축으로 좁힘)이라 결이 어긋나 있었고, 옛 문장 첫머리
  "숫자를 많이 모으기보다"는 그 선택을 오히려 지웠다. 출처 선택을 앞에 두고 실패 시
  지어내지 않는다는 원칙을 뒤에 남겼다. 경력기술서 마지막 쪽 채움 0.862 → 0.857, 3쪽 유지.
- **자기소개서 다섯 값은 그대로 뒀다.** 첫 문장·마지막 문장은 원래 `null`로 비워 둔 칸을
  `01c1098`에서 본인 확정 문장으로 채운 것이고, 한 줄 소개도 `e24e96d`에서 같은 방식으로
  들어왔다. 토막 제목 셋만 `bb5a67f` 재편집에서 새로 생긴 것이어서 처음엔 유지했다가,
  본인 말에 더 가깝게 본문 문장에서 다시 뽑았다. 제목 줄에는 능력 이름이 함께 찍히므로
  제목이 그 능력을 가리키게 골랐다(BRA-C02). 파란 요약줄이 사건을 말하니 제목은 의미를
  말하게 하고 요약줄과 같은 말은 피했다.
  - 고난: 남은 자원까지 책임지는 일을 배웠습니다 · 자기조절력
  - 다시 일어난 날: 말 대신 문서와 리뷰로 기준을 세웠습니다 · 대인관계력
  - 더 나아진 지금: 결과만 맞추지 않고 확인 순서까지 기록합니다 · 자기동기력
  "대학 때 끝내지 못한 서버 연결을 마쳤습니다"도 후보였다. 고난과 이어지는 맛은 있지만
  옆에 붙는 대인관계력과 어긋나서 뺐다. 자기소개서는 1쪽(채움 1.0)을 유지하고, PDF에서
  세 제목이 모두 한 줄에 서는 것을 확인했다.

제출 기준도 다시 봤다. 제출 ZIP을 빈 폴더에 풀어 venv가 아닌 시스템 Python 3.13으로
`device/README.md` 순서대로 돌렸다. 출력 SHA-256 셋이 저장소 실행 결과와도
`device/last-result/`와도 같아 표준 라이브러리만으로 동작한다는 것을 확인했다. 비밀값
검사에서 PDF 셋이 전화번호 패턴에 걸렸으나 압축 스트림의 우연한 바이트였고, DOCX 실제
글자에는 전화번호도 이메일도 없다. 연락 수단은 GitHub 프로필 하나다.

완주 체크리스트의 마지막 빈칸이던 "소설에서 본편을 뽑고 각색한 대목을 사실로 되돌렸다"는
본인이 원고와 대조해 사실임을 확정했다. 11번 소설 원고는 개인정보 경계 때문에 이 저장소에
없어 다른 방법으로는 확인할 수 없는 칸이었다. 이로써 일곱 칸이 모두 찼다.

### 학원 PC 환경 차이

전역 `core.autocrlf=true`가 파일을 CRLF로 바꿔 테스트 1개와 `docs/index.html`의 캐시 해시가
어긋났다. 저장소 로컬만 `autocrlf=false`·`eol=lf`로 두고 다시 체크아웃해 해결했다. 집 PC의
Codex 런타임 Python이 없어 저장소에 `.venv`를 만들고 python-docx와 Pillow를 넣었다.
인계 문서 4절의 명령에서 그 경로를 `.\.venv\Scripts\python.exe`로 바꿔 쓰면 된다.

## 2026-09-21 — 전체 점검과 그 결과 고친 셋

사이트 전체를 항목별로 재고, 나온 세 가지를 고쳤다.

점검에서 통과한 것: 외부 링크 13개와 내부 자원 22개 모두 200이며 로그인을 요구하지 않는다.
제목 28개에 단계 건너뜀이 없고 h1은 하나다. 글자 대비는 280곳 × 두 테마에서 위반 0,
최저 4.59:1. 그림 위 글자는 실제 픽셀로 재서 위반 0. 320px부터 1440px까지 아홉 폭에서
가로 넘침과 12px 미만 글자가 없다. 콘솔 오류와 실패 요청이 없고, 그림 22장 모두
width/height가 실제 크기와 같아 화면이 밀리지 않는다.

- **공유 미리보기 그림을 넣었다.** `og:image`가 없어 링크를 붙이면 글자만 떴다.
  `build_og_image.py`가 승인 데이터의 이름·직무·한 줄 소개를 읽어 1200×630 PNG를 그린다.
  `twitter:card`도 `summary_large_image`로 올렸다. WebP가 아니라 PNG인 이유는 링크를 읽어
  가는 쪽 가운데 WebP를 못 다루는 데가 있어서다. 이 명령만 Pillow가 필요하다.
- **누르는 자리 두 곳을 24px로 넓혔다.** `GitHub 프로필` 링크가 79×16과 108×21이었다.
  문장 속 링크는 기준에서 빠지지만 이 둘은 각각 `<dd>`와 `<p>`를 혼자 차지해 예외가 아니다.
  위아래 여백을 5px 주고 바깥 여백을 같은 만큼 빼서 31px이 됐고 보이는 자리는 그대로다.
  누르는 자리끼리 겹치는 곳이 없음을 51개를 대조해 확인했다.
- **남은 JPEG 4장과 PNG 4장을 WebP로 바꿨다.** 1,086KB가 623KB가 됐다. 차트 두 장이
  121KB→25KB, 111KB→21KB로 가장 많이 줄었다. 지연 로딩이 걸려 있어 첫 화면 속도가 아니라
  끝까지 보는 사람의 총 전송량 문제였다. 바꾼 뒤 그림 위 글자 대비를 다시 쟀고 위반은 없다.
- 테스트를 둘 손봤다. 그림 파일 이름을 적어 두던 외부 호스트 검사는 페이지에서 첫 그림을
  찾아 쓰도록 바꿨다. 공유 미리보기는 링크를 붙여 보기 전에는 빠진 걸 모르므로, 파일 존재와
  1200×630과 `summary_large_image`를 검사하는 테스트를 더했다. 테스트 20개.

## 2026-09-21 — 맨 아래에서 카드 셋이 겹쳐 서던 것 수정

- 스크롤을 끝까지 내리면 왼쪽 카드 3·4·5가 모두 54px에 서서 한 장처럼 뭉쳤다.
  지정한 자리는 64·82·100px이었다.
- 원인은 카드의 `margin-bottom`이다. sticky는 요소의 margin까지 포함해 담는 상자 안에
  가둔다. 카드마다 468px 여백이 붙어 있어 멈추는 자리가 그만큼 위로 끌려 올라갔다.
  계산이 맞는지 확인했다. 상자 바닥 852 − 카드 330 − 여백 468 = 54로 실제와 같다.
- 간격을 `margin-bottom`에서 `.build-stack`의 `gap`으로 옮겼다. gap은 어느 요소의
  margin도 아니어서 멈추는 자리를 밀지 않는다. `last-child` 예외 규칙도 필요 없어졌다.
- 겹침 간격을 18px에서 26px로 벌렸다. 28·54·80·106·132·158px로 서고 마지막 카드
  아래끝이 488px이라 낮은 창에서도 여섯 장이 다 보인다.
- 스크롤 1400px부터 끝까지 200px씩 50곳에서 쟀다. 같은 높이에 겹쳐 서는 카드는 없다.
- 회귀 테스트를 더했다. 카드에 `margin-bottom`이 없고 `.build-stack`에 `gap`이 있으며,
  카드 간격이 모두 같고 마지막 카드가 560px 안에 들어오는지 본다.
- 재는 동안 `render_docx.py`가 초안을 렌더링해도 저장소 기록을 덮어써서, 고치지도 않은
  문서가 테스트에서 어긋났다. 공개 폴더를 렌더링했을 때만 기록을 덮도록 막았다.

## 2026-09-21 — 3번 과제를 개선판인 5번 과제로 교체

- 사용자 판단으로 대표작과 경력의 `3번 과제 ALTER / EGO 카드 스튜디오`를 그 개선판인
  `5번 과제 · 대화 없이 넘긴 AI 인계`로 갈아 끼웠다. 자리는 그대로 세 번째다. 순서가 바뀌면
  왼쪽 카드 번호와 문서 차례가 함께 흔들린다.
- 수치는 t05-ai-handoff 저장소 README에서 확인한 것만 쓴다. 고정 검사 10개, 단위 85개,
  Chromium E2E 38개, 인계 문서 7개 항목, 2026.08 - 2026.09. 3번의 70개·28개는 5번의
  회귀 검사에 흡수되어 더 쓰지 않는다.
- 화면 증거를 바꿨다. 원본과 완성 카드를 반반 비교하는 현재 Studio 화면을 첫 장으로 두고
  첫 화면을 둘째로 밀었다. 가져온 `studio-current.png`는 확장자만 png인 JPEG여서 WebP로
  다시 만들었다(1200×767, 68KB). 쓰지 않게 된 `t03-hero.webp`와 `t03-studio.webp`는 지웠다.
- 링크는 셋이다. 사이트 체험·인계 결과 보기·공개 저장소 모두 로그인 없이 200으로 열린다.
- 이력서 2쪽(마지막 쪽 0.815), 자기소개서 1쪽, 경력기술서 3쪽(0.924)으로 다시 렌더링했다.
- 테스트 19개, 릴리스 검증, 반복 실행, ZIP 생성을 통과했다. 사이트에 t03 흔적은 남지 않았고
  깨진 이미지와 콘솔 오류도 없다.
- 능력은 자기동기력으로 달았다. 3번 때 쓰던 자기조절력을 그대로 두면 여섯 중 셋이 겹치고,
  스스로 실험을 설계해 끝까지 끌고 간 쪽이 본인 판단과 더 맞는다.
- 잘된 것만 적지 않는다. 다음 AI가 금지 규칙보다 앞선 AI의 작업 기록을 먼저 읽은 절차 위반을
  `행동`에 한 줄로 남겼다. 이력서에는 나오지 않는 자리라 2쪽을 그대로 지킨다.
  경력기술서는 3쪽, 마지막 쪽 0.862다.

## 2026-09-21 — 왼쪽 카드 여섯 장과 기술 분류 이름 정리

- 왼쪽 카드가 `projects[:4]`로 잘려 2번과 4번이 보이지 않았다. 제한을 걷어내 공개된 작업을
  다 싣고, 다섯째·여섯째 카드 색을 자주와 청록으로 이었다. 테스트도 승인 데이터에서 세도록
  바꿔 과제를 늘렸을 때 조용히 잘리지 않게 했다.
  - 왼쪽 기둥은 본문 높이에 맞춰 늘어나 있어 카드 둘을 더 놓아도 쪽이 길어지지 않는다.
  - 카드 위 흰 글자 열두 자리를 실제 픽셀로 재서 최소 4.38:1을 확인했다. `VIEW ↗`는 알약
    배경까지 겹쳐 계산해야 한다. 빼먹으면 1.28:1로 잘못 나온다.
- 기술 분류 이름을 `Data · AI`, `Infra · Security`로 바꿨다. 이름 안에 가운뎃점이 생겨 목록
  구분과 섞여 보이므로, 이력서에서는 이름과 목록 사이를 탭으로 끊고 네 줄을 한 칸에 세웠다.
- 사이트 왼쪽 카드의 62px 칸은 그대로 둔다. 재어 보니 넓히면 이름은 한 줄이 되지만 목록이
  더 접혀 블록이 218px에서 239px로 길어진다.

## 2026-09-20 — 2번 미니게임과 4번 정보 보드를 경력에 추가

- 사용자 요청으로 두 과제를 경력과 과제 목록에 넣었다. 수치는 각 저장소 README와 공개 화면에서
  확인한 것만 쓴다.
  - 2번 SOC SHIFT:30: React 18·TypeScript, 자동 테스트 291개, 서버 없는 구조, 오탐과 미탐을
    따로 세는 결과 화면. 2026.08.
  - 4번 GAME PULSE: React 19·Node 22, 회귀 테스트 317개, 과제 조건 35개 감사 경로, 장애 5개
    재현과 복구, 원자료에서 화면값까지 대조. 2026.08 - 2026.09.
- 경력 항목이 여섯이 되면서 이력서가 세 쪽으로 늘 수 있어, 앞선 작업 셋은 그대로 자세히 싣고
  나머지 셋은 `그 외 과제`에 제목·기간·결과·링크만 한 덩어리로 줄였다. 이력서는 2쪽을 지킨다.
  경력기술서는 여섯 항목 모두 상황·판단·행동·결과를 담아 3쪽이 됐고 마지막 쪽도 꽉 찬다.
- 화면 증거를 정리했다. 2번 로비·결과 화면과 4번 첫 화면을 가져왔고, 568KB짜리 PNG와 확장자만
  png였던 JPEG를 WebP로 바꿔 40·33·49KB로 줄였다.
- 공개 링크 네 개(게임 실행·정보 보드 체험·두 저장소)가 모두 200으로 열리는 것을 확인했다.

## 2026-09-20 — 문서 3종을 해외 이력서 관례로 재설계하고 PDF를 함께 낸다

- 참고한 관례: 한 열 구성, 표로 본문을 배치하지 않기, 날짜 오른쪽 정렬, 10~12pt와 0.5~1인치
  여백, 수치가 들어간 결과 문장, PDF 배포. 출처는 `planning/RESEARCH.md`에 적었다.
- 머리글을 다시 짰다. 이름을 가장 크게 두고 문서 종류를 오른쪽에 붙였으며, 그 아래 한 줄에
  직무와 확인 링크를 모으고 강조색 선으로 닫았다. 세 문서가 같은 머리글을 쓴다.
- 이력서의 기술 표를 줄 목록으로 바꿨다. 채용 시스템이 표를 잘못 읽는 일이 있고 줄 수도 아낀다.
  항목마다 있던 링크 줄도 한 줄로 모았다.
- 경력기술서의 상황·행동·결과 표를 줄로 바꾸고 `판단`을 새로 넣었다. 대표작 카드에 이미
  확정해 둔 판단 문장을 그대로 가져왔고, 8번 과제만 기록된 행동에서 정리했다(본인 확인 필요).
- 쪽 수를 정리했다. 이력서 3쪽 → 2쪽, 자기소개서 2쪽 → 1쪽, 경력기술서 3쪽 → 2쪽.
  마지막 쪽에 다섯 줄만 남던 이력서와 한 줄만 남던 자기소개서를 없앴다.
- `documents/render_docx.py`를 저장소에 넣었다. README에 적혀 있었지만 파일이 없어 안내대로
  따라 할 수 없던 단계다. LibreOffice가 없으면 Docker 이미지를 만들어 그 안에서 렌더링하고,
  Windows 글꼴 폴더를 연결해 맑은 고딕을 그대로 쓴다.
- 렌더 결과를 `documents/render-manifest.json`에 남기고 테스트가 검사한다. 쪽 수 상한, 마지막
  쪽 채움 25%, 그리고 본문 지문을 확인해 문서를 고치고 렌더링하지 않으면 실패한다.
- PDF 3종을 같이 공개한다. 사이트 문서 칸은 문서마다 DOCX·PDF 두 단추를 두고, 제출 ZIP에도
  PDF를 넣었다. 워드가 없거나 버전이 다른 사람도 같은 지면을 볼 수 있다.

## 2026-09-20 — 작업 방식 단계 표시 개편과 강조색 위 글자색 정리

- `HOW I WORK`의 번호가 점 위에 겹쳐 찍히던 문제를 없앴다. 번호 자체를 원형 배지로 만들어
  사이드바·레일의 번호 배지와 같은 언어로 맞췄다.
- 세 단계를 한 색으로 두지 않고 설계(파랑) → 구현(보라) → 배포(초록)로 옮겨 가게 했다.
  지나온 구간의 연결선은 그 단계 색으로 남고, 아직 오지 않은 단계는 회색으로 물러난다.
  머리말의 현재 단계 표시도 같은 색을 따른다. 상태 구분은 `aria-pressed`와 형제 선택자로만
  만들어 스크립트를 늘리지 않았다.
- 전체 점검 중 다크 테마에서 실제 대비 문제를 찾아 고쳤다. 활성 내비게이션 배지가 밝은
  강조색 위에 흰 글자였다(2.6:1). 강조색 위 글자색을 테마별로 뒤집는 `--on-accent`를 두어
  7.28:1로 올렸다. 프로필 아바타도 테마를 타지 않는 고정 색 그라데이션으로 바꿨다.
- 사진 위에 얹힌 번호와 `VIEW` 표시는 캔버스로 실제 픽셀을 떠서 확인했다. 어둡게 덮는 층까지
  합성하면 최저 4.27:1로 기준을 넘는다. 계산만으로는 오탐이 나므로 이 방법으로 확인했다.
- 두 테마에서 화면에 보이는 글자 813개를 세 단계 상태 모두에서 재검사해 위반 0건을 확인했다.
- 릴리스 검사기의 12px 기준에 맞춰 배지 글자를 12px로 두었다.

## 2026-09-20 — 오른쪽 레일도 섹션 바로가기로, 활성 표시에 이동 애니메이션

- 오른쪽 레일을 이야기 단계 대신 섹션 5개(이야기·숫자·대표작·경력·문서) 바로가기로 바꿨다.
  왼쪽 사이드바 카드와 같은 항목·같은 모양이며, 스크롤하면 양쪽 활성 항목이 함께 움직인다.
- 1280px 이하에서는 레일이 68px로 접혀 번호 배지만 남고, 880px 이하에서는 레일을 감춘다.
  하단 고정 바가 같은 5개를 이미 보여 주기 때문이다.
- 레일도 `.entrances`라서 모바일에서 하단 고정 바 규칙을 그대로 받아 바가 두 개 겹쳤다.
  규칙 순서와 명시도를 올려 고치고, 순서가 뒤집히면 실패하는 테스트를 넣었다.
- 활성 표시를 항목마다 켜고 끄는 대신 하나의 요소가 미끄러지도록 바꿨다. 위치와 크기는
  활성 항목의 상자를 그대로 따라가며, 세로 목록과 가로 하단 바에서 모두 같은 방식으로 움직인다.
  - 관찰자가 처음 반응하기 전에도 보이도록 주소의 앵커나 첫 항목을 기본으로 켠다.
  - 글꼴 적용이나 폭 변화에 맞춰 `ResizeObserver`와 resize 이벤트로 다시 계산한다.
  - JS가 없으면 기존처럼 항목 배경으로 표시된다. 동작 감소 설정에서는 전환이 꺼진다.
- 이야기 단계 관련 생성 함수·CSS·스크립트를 모두 걷어냈다.
- 1440·1200·375에서 라이트·다크를 확인했다. 테스트 18개, 릴리스 검증, 반복 실행, ZIP 생성,
  JS 구문 검사를 통과했다.

## 2026-09-20 — 사이드바 바로가기를 이야기 단계와 같은 모양으로

- 왼쪽 사이드바 바로가기를 STORY PATH 카드와 같은 형태로 바꿨다. 머리글 `SECTIONS 01 — 05`,
  원형 번호 배지, 항목 구분선, 활성 항목의 파란 배지까지 이야기 단계 레일과 맞춘다.
- 두 목록이 같은 CSS 규칙을 공유하도록 선택자를 묶었다. 한쪽 모양을 바꾸면 다른 쪽도 따라간다.
- 마크업을 `<ol>` 목록으로 바꾸고 번호는 `aria-hidden`으로 두어 읽는 순서를 정리했다.
- **모바일 하단 고정 내비게이션이 실제로는 고정되지 않던 문제를 고쳤다.** 부모 `.side`의
  `backdrop-filter`가 고정 위치의 기준 상자를 만들어, 바가 화면이 아니라 프로필 카드 끝에
  붙어 있었다. 뷰포트 812px 아래인 828px에 놓여 화면에 보이지 않았고 배포본도 같은 상태였다.
  모바일에서 해당 blur를 없애 해결했다.
- 데스크톱 1440·1280과 모바일 375에서 라이트·다크를 확인했다. 맨 아래에서 바가 연락처를
  가리지 않으며 가로 넘침은 없다.
- 구조 회귀 테스트를 더해 테스트 18개, 릴리스 검증, 반복 실행, ZIP 생성을 통과했다.

## 2026-09-20 — 문서 3종 내용 보강

- 사실은 새로 만들지 않고 승인 데이터에 이미 있던 확정 값만 문서로 끌어왔다.
- 이력서 맨 앞에 핵심 역량 세 줄을 넣었다. PR 266개·리뷰 72개, 판정 47/47과 테스트 50개,
  CICIDS2017 2,830,743건과 Macro F1 0.9979 → 0.3871을 먼저 보이게 했다.
- 문서 머리글에 `신입 풀스택 개발자`를 넣고, 이력서 연락 항목과 자기소개서 끝에 공개 사이트
  주소를 넣었다. 문서만 따로 받은 사람도 사이트로 갈 수 있다.
- 학력을 추가했다. 전공은 11번 사실 원장 F01의 확정 값이고, 졸업 연도는 본인이 확인해 주었다.
  대학명은 본인 결정에 따라 넣지 않는다.
- 경력 항목마다 `담당`을 넣어 역할과 별도로 직접 만든 범위를 적었다.
- 3번 과제 ALTER / EGO를 네 번째 경력으로 넣어 사이트 대표작과 문서 구성을 일치시켰다.
  단위 70개·E2E 28개·런타임 의존성 2개는 해당 저장소 README에서 확인했다. 항목이 넷이라
  능력 하나가 겹치며, 범위를 줄이고 스스로 검증한 흐름을 근거로 자기조절력을 달았다.
- 10번 과제에 논문 링크를 넣었다. 그동안 저장소 링크만 있었다.
- 자기소개서 본문은 과제가 정한 구조이고 첫 문장과 마지막 문장이 본인 문장이므로 고치지 않았다.
- T03 경력 카드의 갤러리가 한 칸 비어 Studio 화면을 한 장 더 가져왔다.
- 릴리스 허용 목록 테스트가 사이트 이미지 개수를 9로 고정하고 있어, 실제 `docs/assets` 폴더와
  대조하도록 고쳤다. 이미지를 늘려도 테스트가 깨지지 않는다.
- 테스트 17개, 반복 실행, 최종 사이트 빌드, 릴리스 검증, ZIP 생성을 통과했다.
  데스크톱과 모바일에서 가로 넘침과 12px 미만 글자가 없고, 표는 A4 본문 폭 안에 들어간다.
- **남은 일**: 분량이 늘었으므로 문서 3종을 다시 렌더링해 쪽 나눔을 확인해야 한다.

## 2026-09-20 — 최종 공개 빌드

- 맑은 고딕을 `ascii`, `hAnsi`, `eastAsia`에 지정해 이력서·자기소개서·경력기술서를 다시 만들었다.
- 격리된 LibreOffice 환경에 맑은 고딕을 연결해 문서 3종 6페이지를 렌더링하고 전 페이지를 확인했다.
  - 제목 스타일의 파란 아래선을 제거했다.
  - 자기소개서의 세 번째 장면 제목과 본문이 페이지 사이에서 갈라지지 않도록 묶었다.
  - 글자 깨짐, 겹침, 표 잘림, 링크 이상이 없음을 확인했다.
- 승인 데이터를 최종 상태로 전환하고 `docs/files/`의 문서 3종과 다운로드 링크를 공개했다.
- 데스크톱 1440px과 모바일 390px에서 최종 사이트를 순차 스크롤하며 시각 검수했다.
  - 최소 표시 글자 12px, 가로 넘침 없음, 콘솔 오류 없음, 초안 표시 없음으로 확인했다.
- 단위 테스트 14개, 반복 실행, 외부 링크, 최종 릴리스 검사, DOCX 접근성 검사를 통과했다.
- 공개 합성 입력의 `device/last-result/`를 고정 기대 결과와 대조하고 제출용 ZIP을 생성했다.

## 2026-09-20 — 프론트엔드 표현력 중심 UI 개편

- 기존 D2의 정보 구조는 유지하고 시각 시스템을 전면 재설계했다.
  - 전기색 파랑과 민트 신호색, 반투명 표면, 배경 그리드를 토큰으로 정의했다.
  - 데스크톱은 프로필 패널을 고정하고 본문을 읽는 편집형 레이아웃으로 구성했다.
  - 모바일은 한 열 카드와 하단 고정 5개 섹션 내비게이션으로 전환한다.
- 대표작에 상태와 순번이 보이는 시각 패널을 추가하고, 숫자는 실제 비율로 진행 막대를 만든다.
- 이야기는 세 장면 타임라인, 경력은 STAR 카드, 문서는 다운로드 카드로 위계를 분리했다.
- 바닐라 JS로 다음 점진적 향상을 구현했다.
  - 스크롤 진행률과 현재 섹션 표시
  - 한 번만 실행되는 진입 애니메이션
  - 시스템 설정을 따르고 선택을 저장하는 라이트·다크 테마
- JS가 없어도 모든 콘텐츠가 먼저 보이며, 동작 감소 설정과 인쇄 스타일을 지원한다.
- 데스크톱 1440×1000에서 첫 화면을 시각 검수하고, 모바일 긴 영문 토큰의 최소 폭 문제를 수정했다.
- 사이트 생성, JS 구문 검사, 단위 테스트 11개, 초안 릴리스 구조 검사를 통과했다.
- 기존 공개 포트폴리오에서 개인정보가 가려진 CLOV 화면 4장을 재사용해 첫 경력 카드에 연결했다.
  - 일정 계획, 추억 피드, 우정공간, 상점 화면을 실제 구현 증거로 보여 준다.
  - 데스크톱은 2열 갤러리, 모바일은 가로 스냅 갤러리로 전환한다.
  - 이미지 크기를 명시하고 지연 로딩하며, 모든 이미지에 설명형 대체 텍스트를 넣었다.
- 공유·검색 결과의 제목과 설명이 일관되도록 canonical, Open Graph, Twitter 메타데이터를 추가했다.
- 릴리스 검사기가 대체 텍스트 없는 이미지를 차단하도록 강화했다.
- 사용자가 제공한 T01~T11 저장소를 확인해 제출 숫자를 `11 / 11개`로 확정했다.
  - T01~T08과 T10의 공개 연구 저장소까지 9개 과제는 로그인 없이 검증할 수 있다.
  - T09와 T11은 비공개 저장소로 존재를 확인했으며 공개 링크에는 포함하지 않는다.
  - 저장소만으로 마감 준수 여부를 증명할 수 없으므로 기존 `기한 안에 낸 과제` 표현은
    `제출한 과제 / 현재 제출 대상 과제`로 고쳤다.
  - 분모는 진행 중인 T12와 대기 중인 T13을 제외한 T01~T11이다.
- 사용자가 2026-09-20 현재 이야기 전체와 AWS 결제 금액 134,772원·94,138원의 공개를
  명시적으로 승인했다. 제출문의 본인 판단 문장에서 `[확정 필요]` 표시를 제거했다.
- 대표작은 완료된 결과만 보여 주도록 CLOV와 T10 연구로 재구성했다. 대기 중인 T13은
  완료 전까지 대표작 영역과 최종 공개 차단 조건에서 제외한다.
- T11에서 확정한 AWS·CLOV·ALEPH 장면을 자기소개서 본문으로 사용하되, 소설식 장 제목은
  책임·협업·학습을 드러내는 채용 문서형 제목으로 바꿨다. 렌더러가 없는 환경에서는 생성물을
  `documents/out/` 초안으로만 보관하고 사이트에 공개하지 않는다.

## 2026-09-19 — 전체 재설계

- 기존 요구사항·사이트 골격·이야기 초안·배포 상태를 다시 검토했다.
- 현재 공개 사이트가 404이고, 문서 3종·장치·제출 패키지가 아직 없음을 확인했다.
- 학원 PC에서 받은 최신 비공개 입력은 Git 제외 대상이어서 현재 컴퓨터에 내려오지 않았음을 확인했다.
- 새 단일 기준 문서 `planning/T12-MASTER-PLAN.md`를 작성했다.
  - 승인 콘텐츠를 `content/approved.json` 한 곳에서 관리한다.
  - 공개·비공개·제출 데이터를 분리한다.
  - 이야기 순서를 AWS 고난 → CLOV 회복 → ALEPH 현재로 고정한다.
  - 사이트·문서·장치·검사·릴리스를 다섯 게이트로 나눠 완성한다.
- 기존 설계와 초안은 근거 기록으로 보존한다.

## 2026-09-19 — 공통 기반 구현

- `content/approved.json`에 공개 가능한 사실과 미확정 값을 분리했다.
- `device/build_site.py`로 승인 데이터에서 정적 사이트를 생성한다.
  - AWS 고난 → CLOV 회복 → ALEPH 현재 흐름을 반영했다.
  - 로그인 필요한 CLOV 서비스 주소를 제거하고 공개 저장소만 연결했다.
  - 미확정 값은 초안 모드에서만 노란 표시로 보인다.
- `device/refresh.py`로 리추얼·출석·제출 기록 집계와 문장 후보 생성을 구현했다.
- `device/check_repeat.py`가 합성 입력을 새 폴더 두 곳에서 실행하고 SHA-256을 비교한다.
- `device/validate_release.py`가 미확정 값, 없는 링크, 로그인 URL, 비밀값 의심 문자열을 검사한다.
- `release/build_release.py`가 최종 검사 통과 뒤 허용 목록만 ZIP으로 묶도록 만들었다.
- `documents/build.py`가 승인 데이터에서 이력서·자기소개서·경력기술서 DOCX를 생성한다.
  - 내부 초안 3개는 구조 검사를 통과했다.
  - 현재 PC에 LibreOffice·Word 렌더러가 없어 페이지 이미지 시각 검수는 보류했다.
  - 최신 이야기로 초안을 다시 만들고 섹션·제목·접근성을 검사했다. 이력서의 원시 URL 링크는
    `GitHub 프로필`이라는 설명형 링크로 바꿨다.
- 단위 테스트 11개와 반복 실행 기대 결과 비교가 통과했다.
- 최종 빌드와 ZIP은 본인 확인 항목이 남아 있어 의도대로 차단된다.
- `device/apply_numbers.py`를 추가해 집계값을 미리보기로 확인한 뒤 승인 데이터에 반영하도록 했다.
- GitHub Actions에서 Python 컴파일, 단위 테스트, 반복 실행, 초안 빌드를 자동 검사한다.
- GitHub Actions의 Node 20 사용 중단 경고를 없애기 위해 공식 최신 주 버전인
  `actions/checkout@v7`과 `actions/setup-python@v7`로 갱신했다.
- 외부 URL은 HEAD가 아닌 GET 본문을 확인해, 껍데기 응답만 200인 장애 링크도 잡도록 강화했다.
- 최종 ZIP에 숫자 승인 도구를 포함하고 `README-FIRST.md`를 ZIP 최상단에 두도록 허용 목록을 바로잡았다.
- 세 번째 경력 항목은 공개 증거가 있는 T08 패스키 과제로 정했다.
  - 판정 47/47, 자동 테스트 50개, 비밀번호 입력 칸 0개를 공개 README와 저장소에서 확인했다.
  - 공개 앱은 2026-09-19 GET 요청에 503을 반환해 링크에서 제외하고, 정상화 뒤 다시 넣기로 했다.
- T09 최종 작업본에서 사용자가 직접 선택했다고 명시된 첫 문장과 마지막 문장을 가져왔다.
- 기존 공개 포트폴리오의 공개 범위 결정에 따라 GitHub 프로필을 연락 수단으로 가져왔다.
- 기존 공개 포트폴리오의 첫 화면 문장 `백엔드의 구조를 설계하고, 화면까지 완성합니다.`를
  과제 형식인 `…한 사람`에 맞게 최소 편집해 한 줄 소개로 가져왔다.
- 실제 일정 근거가 필요한 T13 예정일은 여전히 비워 두었다.
- 로컬 브라우저에서 데스크톱 937×592와 모바일 390×844 레이아웃을 확인했다.
  - 두 화면 모두 가로 넘침이 없었다.
  - 모바일의 숫자·대표작·경력 카드가 한 열로 정상 배치됐다.
  - 12px 미만의 표시 텍스트와 브라우저 콘솔 경고·오류가 없었다.
- 로컬 서버 로그에서 파비콘 404를 발견해 SVG 파비콘을 추가했다.
- 릴리스 검사기가 CSS·파비콘·이미지·스크립트 같은 필수 사이트 자산의 누락도 검사한다.
- T11 사실 원장의 확정 항목만 사용해 이야기 세 장면을 1,400~1,700자 기준에 맞게 보강했다.
- 릴리스 검사기가 이야기 분량과 한 줄 소개의 `…사람` 형식을 검사한다.

## 2026-09-17 — 저장소 생성과 설계 준비

- `myeongjundev/about`을 public으로 만들었다. 사이트는 GitHub Pages(`https://myeongjundev.github.io/about/`)로 공개할 예정이다.
  기존 포트폴리오 `myeongjundev.github.io`는 그대로 둔다.
- 동기에게 받은 과제 안내 텍스트로 요구사항과 통과 기준을 정리했다(`planning/REQUIREMENTS.md`).
  원문 파일은 저장소에 넣지 않는다.
- **원문 텍스트에 붙어 있던 리추얼 기록(27일, 동료 칭찬·감사)은 동기의 기록이다. 쓰지 않는다.**
  본인 과제 화면이 열리면 본인 리추얼 JSON을 받는다.
- 설계 초안 작성(`planning/DESIGN.md`): 사이트 구조, 이야기 뼈대 후보, 되돌릴 각색 목록, 숫자 칸 후보, 장치·문서 설계.
- 재사용할 자료: 9번 서사 문서(`t09-self-introduction/drafts/T09-13-서사문서-본문.md`), 공개 도구
  `ritual-strength-agent`, 11번 사실 원장(`t11-autobiographical-novel/planning/FACT-LEDGER.md`), 10번 공개 저장소
  `explainsoc-research`.

## 2026-09-17 — 사이트 조사와 디자인 방향

- 개발자 자기소개·이력서 사이트 조사(`planning/RESEARCH.md`).
- Claude Design 프롬프트 정리(`planning/DESIGN-PROMPT.md`). 첫 시안(카드형)은 "AI가 만든 결과물 같다"는 이유로 채택하지 않았다.
- 해외 개발자 사이트 6곳의 색·글꼴·구조를 브라우저에서 실측했다. 이를 바탕으로 A·B·C 세 방향과 국내·해외 겸용 신뢰형 D를 만들었다.
- D를 T12 채점 조건에 맞춰 고친 **D2**(데스크톱·모바일)를 채택했다. 결정 내용과 미정 사항은 `planning/DESIGN-DECISION.md`에 적었다.
  - 시안 캔버스: https://claude.ai/artifact/CbHUdWUnN9yoNSuhcTfwPD
  - 시안 생성 스크립트 사본: `planning/design-canvas/gen_d2.py`

## 2026-09-18 — 기록 받기와 D2 뼈대

- 본인 리추얼 JSON과 출석 기록을 받아 `inputs/`에 두었다(Git 제외).
  - 출석: 수업일 27일 중 27일, 지각·결석 0 (09-17 기준)
  - 아침 리추얼: 27/27 수업일
- 과정 기간은 2026-08-11부터 11-12까지다(DESIGN.md 5절).
- `docs/`에 D2 구조로 정적 뼈대(`index.html`, `styles.css`)를 만들었다.
  - 아직 채우지 않은 자리는 `.todo`(노란 배경)로 표시했다.
  - 숫자 칸 두 개와 10번 논문(제목·요약·PDF 링크)은 실제 값을 넣었다.
  - 데스크톱은 1120px 폭에서 왼쪽 360px·오른쪽 본문, 900px 이하는 한 줄로 쌓인다.

## 다음 작업

**T12는 2026-09-21에 제출했다.** 제출 기록과 제출 뒤 할 일은
[`SUBMISSION-2026-09-21.md`](SUBMISSION-2026-09-21.md)에 있다.

[`HANDOFF-2026-09-21.md`](HANDOFF-2026-09-21.md)의 1~3번은 09-21에 모두 처리했다. 인계
문서는 그때의 판단 근거가 남아 있으니 기록으로 둔다.

`REQUIREMENTS.md`의 완주 체크리스트 일곱 칸이 모두 찼다. 제출에 필요한 일은 남지 않았다.

언제든 되돌아올 수 있는 자리만 적어 둔다.

- 자기소개서 토막 제목 셋. 09-21에 본문 문장에서 다시 뽑았다. 또 고친다면 자기소개서가
  꽉 찬 1쪽이라 제목이 두 줄로 밀리면 2쪽이 된다. 공백 빼고 23자 안쪽으로 쓰고
  `HANDOFF-2026-09-21.md` 4절의 일곱 단계를 전부 돌린 뒤 1쪽인지 확인한다.
- 대표작의 T13 앱 자리. 09-21에 예정 카드로 되살렸다(예정 · 2026.11.12 이전). 13번을 마치면
  `approved.json`의 `t13-app`을 `published`로 바꾸고 이름·그림·링크·검증 결과를 채운다.
